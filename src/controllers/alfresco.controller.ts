import * as Express from 'express';
import APIError from '../helpers/errorHandlers/ApiError.helper';
import { parseString } from 'xml2js';
import { alfrescoConfig } from '../config/config';

function getAlfrescoAccessTokens(req: Express.Request, res: Express.Response) {}
